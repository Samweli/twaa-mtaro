#IWCONTENT
  #loader{:style => "display: none;"}
    = image_tag "ajax-loader.gif"
  #info_window
    =# devise_error_messages!.inspect
    - flash.each do |key, message|
      %div{:id => key}
        = message
    = yield
