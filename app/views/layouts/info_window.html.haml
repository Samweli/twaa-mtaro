#IWCONTENT
  #loader{:style => "display: none;"}
    = image_tag "ajax-loader.gif"
  #info_window
    - if false
      - flash.each do |key, message|
        %div{:id => key}
          = message
    = yield
