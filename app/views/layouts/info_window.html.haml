#IWCONTENT
  #loader{:style => "display: none;"}
    = image_tag "ajax-loader.gif"
  #info_window
    - unless defined?(devise_error_messages!)
      - flash.each do |key, message|
        %div{:id => key}
          = message
    = yield
